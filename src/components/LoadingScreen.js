import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { AudioWaveform } from 'lucide-react-native';
import { C, RADIUS } from '../theme';
export default function LoadingScreen({ progress, error }) { const pct = Math.round((progress.loaded / Math.max(1, progress.total)) * 100); return <View style={styles.wrap}><View style={styles.badge}><AudioWaveform size={34} color={C.cyan} /></View><Text style={styles.title}>BEATFORGE</Text><Text style={styles.sub}>MPC PAD · STEP SEQ · DJ REMIX</Text>{error ? <Text style={styles.error}>{error}</Text> : <><View style={styles.bar}><View style={[styles.fill, { width: `${pct}%` }]} /></View><Text style={styles.status}>LOADING SAMPLES {progress.loaded}/{progress.total} · {pct}%</Text><Text style={styles.label}>{progress.label}</Text></>}</View>; }
const styles = StyleSheet.create({ wrap: { flex: 1, backgroundColor: C.bg, alignItems: 'center', justifyContent: 'center', padding: 32 }, badge: { width: 78, height: 78, borderRadius: RADIUS.xl, borderWidth: 1.5, borderColor: C.cyan, backgroundColor: C.panel, alignItems: 'center', justifyContent: 'center', marginBottom: 22 }, title: { color: C.text, fontSize: 24, fontWeight: '900', letterSpacing: 2 }, sub: { color: C.textFaint, fontSize: 10, fontWeight: '700', letterSpacing: 2, marginTop: 6, marginBottom: 34 }, bar: { width: '100%', maxWidth: 300, height: 6, borderRadius: 3, backgroundColor: C.panelHi, overflow: 'hidden' }, fill: { height: '100%', backgroundColor: C.cyan }, status: { color: C.textDim, fontSize: 10, fontWeight: '800', letterSpacing: 1.2, marginTop: 14 }, label: { color: C.textFaint, fontSize: 9, marginTop: 4 }, error: { color: C.red, textAlign: 'center' } });
