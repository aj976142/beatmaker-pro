# Audio assets

The original BeatForge archive contains the generated WAV sample bank under `assets/samples/`.

The GitHub connector used for this import can commit source/text blobs directly, but the binary WAV assets were not transferred in this bootstrap commit. Keep the original project archive as the source for these files until they are uploaded through a normal Git client.
